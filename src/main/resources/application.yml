spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/student
    username: ""
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
server:
  error:
    include-message: always
#googleClassroom:
#  api:
#    endpoint: https://classroom.googleapis.com
#  credentials:
#    file-path: /secrets/google-credentials.json
#  redirect-uri: http://localhost:8080/oauth2/callback
